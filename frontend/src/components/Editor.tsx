import { useEffect, useRef } from "react";

const Editor = ({ onChange, editorLoader, name, value }) => {
  const editorRef = useRef();

  useEffect(() => {}, []);

  return <div>Editor</div>;
};

export default Editor;
